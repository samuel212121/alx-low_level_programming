#include <stdio.h>
/**
 * main - print all single digit number base 10
 * follow new line
 * Return: Always 0 (Success)
 */
int main(void)
{
	int x;

	for (int x = 48; x < 58; x++)
	{
		putchar(x);
	}
	putchar('\n');
	return (0);
}
