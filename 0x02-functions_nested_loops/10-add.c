#include "main.h"
/**
 * add - adds two intger and return the result
 * @x:int to be added to y
 * @y:int to be added to x
 * Return:sum of a and b
 */
int add(int x, int y)
{
	int z;

	z = x + y;
	return (z);
}
