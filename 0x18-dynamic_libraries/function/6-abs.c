#include "main.h"
/**
 * _abs - compute the absolute value of an intger
 * @n:int n to check
 * Return:the absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
